extends layout

block content
    h1 Все курсы Академии

    form(method="post" action='/corses/add')
        fielset.form-group
            label(for="name") Name:
            input.form-control(name="name", id="name", type="text", placeholder="Введите имя курса")
            small.text-muted Добавьте свой новый курс.
        button.btn.btn-primary(type="submit") Сохранить курс
        a.btn.btn-default(href="/courses") Отмена
